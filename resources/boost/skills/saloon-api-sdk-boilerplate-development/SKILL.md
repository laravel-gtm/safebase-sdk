---
name: saloon-api-sdk-boilerplate-development
description: Build and extend the saloon-api-sdk-boilerplate package — connector, requests, responses, Laravel integration, and tests.
---

# saloon-api-sdk-boilerplate development

This skill has not been populated yet.

## Setup required

Run the **saloon-api-sdk-boilerplate-initial-setup** skill to generate API-specific content for this file. That skill will examine the codebase and produce a comprehensive development guide with real class names, resource methods, code examples, and testing patterns.

Until then, refer to:

- `CLAUDE.md` at the repo root for commands and architecture overview
- `.claude/rules/` for Saloon 4, PHPStan, and Laravel package conventions
- `src/` for the current codebase structure
